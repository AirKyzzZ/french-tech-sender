import { APP_NAME, APP_DOMAIN, APP_HOSTNAMES } from "./site";
import { inter, satoshi } from "./fonts";
import { PERKS } from "./perks";
import { PLAN, STATS, CATEGORIES } from "./plans";
import { REVIEWS } from "./reviews";
import { FOOTER_LINKS, NAV_LINKS, SIDEBAR_LINKS } from "./links";
import { LIST_ITEM_VARIANTS, CHILD_VARIANTS, FADE_IN_VARIANTS, MODAL_VARIANTS } from "./animation";

export {
    APP_NAME,
    APP_DOMAIN,
    APP_HOSTNAMES,
    inter,
    satoshi,
    LIST_ITEM_VARIANTS,
    CHILD_VARIANTS,
    FADE_IN_VARIANTS,
    MODAL_VARIANTS,
    PERKS,
    PLAN,
    STATS,
    CATEGORIES,
    REVIEWS,
    FOOTER_LINKS,
    NAV_LINKS,
    SIDEBAR_LINKS,
};
